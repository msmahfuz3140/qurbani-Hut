import AnimalsClient from "@/components/AnimalsClient";

export const metadata = {
  title: "All Listings — QurbaniHut",
  description:
    "Browse cows, bulls, and goats for Eid Qurbani. Fair pricing in BDT, clear categories, humane listing standards.",
};

const AnimalsPage = () => {
  return <AnimalsClient />;
};

export default AnimalsPage;
